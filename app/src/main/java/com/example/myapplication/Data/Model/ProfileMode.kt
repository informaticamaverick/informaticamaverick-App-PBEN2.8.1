package com.example.myapplication.data.model

/**
 * Modo de perfil del usuario
 */
enum class ProfileMode {
    CLIENTE, EMPRESA
}

/**
 * ProfileMode: Enum que define el modo de visualización del perfil (CLIENTE o EMPRESA).
 */
