package com.example.myapplication.prestador.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val purpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Color principal del prestador - Naranja (mantener compatibilidad)
val PrestadorOrange = Color(0xFFF97316)
val PrestadorOrangeDark = Color(0xFFEA580C)

// Paleta de colores para pantalla de registro (estilo más suave y consistente)
val OrangeLight = Color(0xFFFB923C)      // Naranja claro para acentos
val OrangePrimary = Color(0xFFF97316)    // Naranja principal
val OrangeDark = Color(0xFFEA580C)       // Naranja oscuro para hover/activo

// Colores de fondo y texto
val BackgroundLight = Color(0xFFFFF5ED)  // Fondo crema claro (igual al login)
val SurfaceWhite = Color(0xFFFFFFFF)     // Blanco puro
val SurfaceGray = Color(0xFFF8FAFC)      // Gris muy claro
val BorderGray = Color(0xFFE2E8F0)       // Gris para bordes
val TextPrimaryDark = Color(0xFF1E293B)  // Texto principal oscuro
val TextSecondaryGray = Color(0xFF64748B) // Texto secundario gris
val DividerLight = Color(0xFFF0F2F5)     // Divisor claro
val ErrorRed = Color(0xFFEF4444)         // Rojo para errores

// Colores para chips de servicios
val ChipBackground = Color(0xFFFED7AA)   // Fondo naranja muy claro
val ChipText = Color(0xFFC2410C)         // Texto naranja oscuro

// Gradientes (usar colores más suaves)
val GradientStart = Color(0xFFFB923C)    // Naranja medio
val GradientEnd = Color(0xFFFBBF24)      // Amarillo-naranja

